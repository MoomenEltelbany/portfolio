import { PortfolioMenuList } from "../components";

function Main() {
    return (
        <main className="bg-red-500 flex-grow">
            <PortfolioMenuList bare />
        </main>
    );
}

export default Main;
